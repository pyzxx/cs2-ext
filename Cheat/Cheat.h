#include "../memory/memory.h"






inline memify mem("cs2.exe"); // pid

class Reader
{
public:
	uintptr_t client = 0;
	//get client
	void ThreadLoop();
	void Loopcheat();
	
private:
};
inline Reader reader;

class Cheat
{
public:
	
    void Aimbot();
	void RenderESP();
	void screenfov();
private:
   //Vector3 findClosest(const std::vector<Vector3> playerPositions);
    //void MoveMouseToPlayer(Vector3 position);

};
inline Cheat cheat;









